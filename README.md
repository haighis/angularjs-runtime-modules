Angular JS 1.x Runtime Modules

A plugin/module framework that allows for extensible modules that can be changed at runtime. 

Configuration/Installation

```npm install```

```bower install```

```node server.js```
